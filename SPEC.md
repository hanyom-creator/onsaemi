# ============================================================================
#  온새미 (Onsaemi) — 프로젝트 참조 명세
#  ---------------------------------------------------------------------------
#  이 파일은 실행되지 않는다. 전부 주석이다.
#  VS Code 사이드바에 열어두고 보거나, Claude Code 가 읽어 맥락을 잡는 용도.
#
#  버전   : v0.2
#  작성일 : 2026-09-21
#  작성자 : 고하영 (한국폴리텍대학 강서캠퍼스 사이버보안과)
#  WBS    : 1.2.3 API 명세 · SQLite 스키마 · 아키텍처 설계도
# ============================================================================


# ============================================================================
#  1. 프로젝트 개요
# ============================================================================
#
#  이름   : 온새미 (Onsaemi)
#  한 줄  : 보이스피싱 통화 중 실시간으로 위험을 판단하고,
#           피해자가 그대로 읽을 수 있는 대응 스크립트를 화면에 띄우는 앱
#
#  차별점 : 기존 앱은 "이 번호는 위험합니다" 에서 끝난다 (탐지).
#           온새미는 "지금 이렇게 말하세요" 를 준다 (대응).
#           통화 중인 피해자는 이미 설득당한 상태라, 경고만으로는
#           스스로 빠져나오지 못한다는 전제에서 출발한다.
#
#  1인 개발 · 주 24.5시간 · 2026-09-07 착수
#  논문 제출 10/08 · 중간발표 10/13 · 개발 동결 11/22 · 최종발표 12/01
#
#  범위 밖 (명시적 제외)
#    - Lv5 자동 통화 종료 (TelecomManager.endCall() 은 기본 전화앱 권한 필요)
#    - 온디바이스 STT / SLM 전환
#    - iOS 대응 · Play 스토어 배포 심사
#    - 대규모 실사용자 대상 정탐·오탐 검증


# ============================================================================
#  2. 캡처 구조 — 2폰 구성 (S1 실측 결과로 확정)
# ============================================================================
#
#  단일 단말 캡처는 불가능하다. 2026-09-12 실측으로 확인했다.
#
#    A1 MIC 기본            → 무음
#    A2 MIC + AEC 해제      → 무음
#    A3 VOICE_RECOGNITION   → 무음
#    A4 VOICE_COMMUNICATION → 본인 음성만 잡히고 송신 음성 차단
#
#  원인은 AEC 가 아니라, 통화 중 OS 가 마이크를 통신 서브시스템에
#  독점 할당하기 때문이다 (A4 결과가 AEC 가설을 반증한다).
#
#  확정 경로:
#
#      [주폰]  통화 (스피커폰 ON)
#         │ 공기 중 음성
#         ▼
#      [보조폰] 마이크 캡처 → 2초 WAV 청크 → 서버 업로드
#
#  따라서 2.1.x 캡처 모듈은 전부 보조폰 기준으로 구현한다.
#  이 전제는 논문 한계 절(7.3.4)에 반드시 서술한다.


# ============================================================================
#  3. 전체 아키텍처
# ============================================================================
#
#   ┌─────────────┐        ┌──────────────────────────────────────────────┐
#   │   주폰      │        │              서버 (FastAPI)                  │
#   │  스피커폰   │        │                                              │
#   └──────┬──────┘        │   ┌────────────────────────────────────┐     │
#          │ 음성          │   │ ① Google STT  (2초 청크 recognize) │     │
#          ▼               │   └────────────────┬───────────────────┘     │
#   ┌─────────────┐        │                    ▼                         │
#   │   보조폰    │        │   ┌────────────────────────────────────┐     │
#   │             │        │   │ ② 민감정보 파이프라인              │     │
#   │ AudioRecord │        │   │    탐지 → 신호 기록 → 치환         │     │
#   │      │      │        │   └────────────────┬───────────────────┘     │
#   │      ▼      │        │                    ▼                         │
#   │  2초 WAV    │ POST   │   ┌────────────────────────────────────┐     │
#   │   청크      ├───────►│   │ ③ 위험도 산출 + 스크립트 생성      │     │
#   │             │multipart   │    GPT-4o-mini · 65점 게이트       │     │
#   │             │        │   └────────────────┬───────────────────┘     │
#   │  오버레이   │◄───────┤                    ▼                         │
#   │  Lv1~Lv5    │  SSE   │   ┌────────────────────────────────────┐     │
#   └─────────────┘        │   │ ④ SQLite 저장 + SSE 푸시           │     │
#                          │   └────────────────────────────────────┘     │
#                          └──────────────────────────────────────────────┘
#
#  업로드와 결과 수신은 분리돼 있다.
#  업로드는 202 로 즉시 끝나고(0.1초), 분석 결과만 SSE 로 따로 밀어낸다.
#  한 요청에서 분석까지 기다리면 2초 청크 주기가 무너지고 지연이 누적된다.
#
#  종단 지연 목표: 발화 종료 → 화면 표시까지 2~5초
#    청크 대기 0~2s │ 업로드 0.2~0.5s │ STT 0.5~1s │ LLM 1~2s │ SSE 0.1s
#  "실시간" 은 지연 0 이 아니라 "통화가 진행되는 중에" 라는 뜻으로 정의한다.
#  사용자가 계좌번호를 누르기 전에 화면이 뜨는 것이 목표다.


# ============================================================================
#  4. 에스컬레이션 Lv0 ~ Lv5
# ============================================================================
#
#  상세 규칙(점수 구간·상승·하강 전이도)은 docs/LEVEL_STATES.md 를 따른다.
#  가중치·누적 규칙은 docs/RISK_WEIGHTS.md 를 따른다.
#
#   Lv0  감시     0–39    개입 없음                                  무음 모니터링
#   Lv1  알림     40–64   경고 배너                                  대조군 조건
#   Lv2  탐침     65–79   확인 질문 스크립트 제시                    실사용 고려 짧은 대본
#                 ("어느 지점이신가요", "직원번호 알려주세요")
#                 사기범이 답을 못 하면 그 반응이 다시 신호로 잡혀 점수 상승
#   Lv3  거절     80–89   짧은 거절 스크립트 (한 호흡에 말할 수 있는 짧은 문장)  실사용 고려 짧은 대본
#                 ("확인 후 다시 걸게요")
#   Lv4  반론     90–99   사기범 발화마다 맥락 기반 반론, 길이 제한 없음  AI 대본 생성 능력 검증
#   Lv5  종료     100/강제  통화 종료 버튼 + 사후 보호                최종 안전장치
#
#   65점 미만이면 스크립트를 출력하지 않는다 (임계값 게이트, 3.2.2).
#   단, 강제 승급(sensitive_pattern, critical_repeat)으로 Lv5가 된 경우는
#   65점 미만이어도 화면을 출력한다.
#
#   Lv5 사후 보호: 원터치 신고 · 보호자 알림 · 재접촉 방어 · 지급정지 안내
#   (설계로만 제시. 구현은 논문 제출 이후)
#
#   상승 조건:
#     1) 점수가 상위 구간 진입 (구간 건너뛰기 허용)  → level_reason = "score"
#     2) Lv2 탐침 30초 미응답                        → level_reason = "timeout"
#     3) 사용자 측 민감정보 발화                      → 즉시 Lv5, "sensitive_pattern"
#     4) 정말 위험한 말 3회 (종류 무관)                → 즉시 Lv5, "critical_repeat"
#
#   하강 규칙:
#     신호 없는 청크가 30초 연속이면 한 단계 내려가고, 점수는 새 레벨의
#     최저점으로 맞춘다. 한 번이라도 Lv2 이상에 도달한 통화는 Lv2 아래로
#     내려가지 않는다. Lv5는 내려가지 않는다.


# ============================================================================
#  5. 민감정보 처리 — 두 기능의 목적이 다르다
# ============================================================================
#
#  혼동 주의. 마스킹과 에스컬레이션은 지키는 대상이 정반대다.
#
#    마스킹        : 사용자를 "우리 시스템" 으로부터 보호
#                    통화 내용이 해외 LLM API 로 국외 이전되는 것을 최소화
#    에스컬레이션  : 사용자를 "사기범" 으로부터 보호
#                    민감정보를 넘기기 전에 개입하는 것이 본래 목적
#
#  마스킹 파이프라인은 3단계다. 탐지와 치환을 분리해,
#  값은 지우되 "그런 발화가 있었다" 는 신호는 남긴다.
#  한 번에 치환해버리면 GPT 가 받는 텍스트에서 위험 신호가 사라진다.
#
#      STT 결과 → ① 패턴 탐지 → ② 신호 기록 → ③ 치환 → LLM 전달
#
#    사기범 측 패턴 : 수취 계좌번호, 기관 사칭 전화번호   → 위험점수 가산
#                    수취 계좌번호는 '수취계좌제시' 신호(+15)로 명시한다
#    사용자 측 패턴 : 카드번호, 주민번호, CVC, 인증번호   → Lv5 강제 승급
#
#  1차 방어는 사기범 압박 신호 누적을 통한 선제 승급이다.
#  사용자 측 패턴 감지는 이미 새어나간 뒤의 최후 방어선이며,
#  이 시점의 스크립트는 "속지 마세요" 가 아니라
#  카드 정지·지급정지 절차 안내여야 한다 (1.3.1 에서 화면 분기).


# ============================================================================
#  6. API 명세 요약
# ============================================================================
#
#  Base URL (개발) : https://<ngrok-id>.ngrok-free.app
#  Base URL (운영) : https://<ec2-ip>.nip.io
#  시각 형식       : ISO 8601  (2026-09-15T14:32:10+09:00)
#
#  인증: X-API-Key 헤더
#        환경변수 API_KEY 가 비어 있으면 검증 생략 (ngrok 개발 구간)
#        값이 있으면 대조 후 불일치 시 401       (EC2 운영 구간)
#        코드는 처음부터 넣어두고 EC2 이관 때 값만 채운다.
#
#  세션 ID: 앱이 통화 감지 즉시 UUID v4 생성.
#           서버는 처음 보는 ID 가 오면 세션을 자동 생성한다.
#           별도 생성 엔드포인트를 두지 않는다 — 첫 청크 전 왕복 1회 제거.
#
# ----------------------------------------------------------------------------
#  6.1  POST /sessions/{session_id}/chunks
# ----------------------------------------------------------------------------
#  요청: multipart/form-data
#        seq          int    청크 순번 (0부터)
#        recorded_at  str    녹음 시작 시각 (ISO 8601)
#        audio        file   WAV 16kHz mono 16bit, 2초
#
#  WAV 원본을 변환 없이 보낸다. Base64 는 쓰지 않는다
#  (용량 33% 증가 + 인코딩·디코딩 지연이 종단 지연 수치를 오염시킨다).
#
#  응답: 202  { "seq": 7, "accepted": true }
#        분석을 기다리지 않고 즉시 응답. 결과는 SSE 로 간다.
#
#  전제: 침묵 구간에도 청크를 계속 보낸다.
#        타임아웃 기준이 "소리" 가 아니라 "청크" 이기 때문이다.
#        무음 스킵 최적화를 넣으려면 별도 heartbeat 가 먼저 필요하다.
#
#  재시도:
#        1) 지수 백오프 즉시 재시도 — 1s → 2s → 4s (최대 3회)
#        2) 3회 실패 시 로컬 큐에 적재하고 다음 청크로 진행 (통화를 막지 않는다)
#        3) 복구되면 큐를 seq 오름차순으로 전송
#        4) 서버는 도착 순서가 아니라 seq 기준으로 정렬해 문맥을 만든다
#
#  지연 도착 청크:
#        일반 위험 신호      → 점수에만 반영, 스크립트 미발송
#        사용자 민감정보     → 지연 여부 무관하게 즉시 Lv5
#
# ----------------------------------------------------------------------------
#  6.2  GET /sessions/{session_id}/stream        (SSE)
# ----------------------------------------------------------------------------
#  통화 시작 시 1회 연결한다. 서버가 결과를 생성할 때마다 밀어낸다.
#
#  {
#    "seq": 7,
#    "transcript": "안전계좌 302-****-****로 이체하셔야 합니다",  # 마스킹본
#    "risk_score": 82,          # 누적 총점
#    "level": 3,                # 0~5
#    "level_reason": "score",   # score | timeout | sensitive_pattern | critical_repeat
#    "signals": ["기관사칭", "수취계좌제시", "이체요구"],
#    "script": "확인 후 다시 걸게요",   # 65점 미만이면 null
#                                                # (단, sensitive_pattern·critical_repeat 강제 승급 시는 예외)
#    "script_followed": null,   # yes | no | alternative | dismissive
#    "latency_ms": 1840         # 6.3.3 종단 지연 지표
#  }
#
#  script_followed — 직전 스크립트 이행 판정
#     yes          문구가 발화에 나타남                    → 위험도 유지
#     no           미이행, 사기범 요구대로 응답            → +15점
#     alternative  자기 방식으로 대응 (거절·종료 시도)     → 위험도 유지
#     dismissive   무의미·비협조 응답 ("꺼지세요")         → 기록만, 판정 미반영
#
#  판정 시간은 docs/LEVEL_STATES.md 공식을 따른다.
#  판정은 문구 매칭 + 사기범 반응 변화 판단을 병행한다.
#  dismissive 를 로직에 반영하지 않는 이유:
#    오탐이 나면 "보호를 낮추는" 방향이기 때문이다.
#    실제 사기 통화에서 홧김에 한 말을 장난으로 오판하면 개입이 사라진다.
#    보호 수준을 낮추는 자동 판단은 이번 단계에서 배제한다.
#    (원칙: 올리는 판단보다 내리는 판단을 훨씬 보수적으로)
#
# ----------------------------------------------------------------------------
#  6.3  POST /sessions/{session_id}/end
# ----------------------------------------------------------------------------
#  요청: { "ended_at": "...", "last_seq": 84 }
#  응답: { "session_id", "total_chunks", "max_risk_score", "max_level" }
#
#  세션 종료 3경로:
#     app_notified    POST /end 수신              정상 경로, 즉시
#     chunk_timeout   마지막 청크 후 60초         앱 강제종료·네트워크 단절 대비
#     session_limit   세션 생성 후 5분            API 비용 방어 (5.1.3)
#
# ----------------------------------------------------------------------------
#  6.4  오류 코드
# ----------------------------------------------------------------------------
#     400  필수 필드 누락, seq 형식 오류
#     401  API 키 불일치
#     404  종료된 세션에 청크 전송
#     413  청크 크기 초과
#     500  STT · LLM 호출 실패
#
#     { "error": "invalid_api_key", "message": "API 키가 올바르지 않습니다" }


# ============================================================================
#  7. DB 스키마 (schema.sql)
# ============================================================================
#
#  총 5개 테이블: session, experiment, utterance, script, feedback
#
#  session      통화 단위. session_id(PK) 는 앱이 만든 UUID 문자열
#  experiment   실험 조건. session 과 1:1. 운영 로직과 무관
#               input_mode(recording|live_call) · response_mode(ctrl_lv1|exp_full)
#               · scenario_id · trial_no · notes
#               별도 테이블로 분리한 이유: 11월 본 실험에서 항목이 늘어도
#               session 을 건드리지 않기 위해
#  utterance    청크 단위 STT 결과. 문맥 구성의 기준 테이블
#               UNIQUE(session_id, seq) 로 재전송 중복 차단
#               score_delta  이 청크에서 오른 점수 → 6.3.2 탐침 효과 지표
#               score_total  누적 총점            → 논문 5장 그래프 y축
#               recorded_at / received_at 분리 → 전송 지연 측정
#               audio_path   실험용 빌드만. 최종 빌드는 NULL
#  script       생성된 스크립트. script_followed 는 나중에 갱신됨
#  feedback     사용자 조작 (report | dismiss | end_call)
#
#  문맥 도출: session_id 로 묶어 utterance 를 seq 오름차순으로 읽어 합친다.
#            2초 한 조각만으로는 판단이 안 되므로 누적 텍스트를 LLM 에 넘긴다.
#
#  하강 시 점수는 새 레벨의 최저점으로 맞춘다. 단, 한 번이라도 Lv2 이상에
#  도달한 통화는 Lv2 아래로 내려가지 않는다 (docs/LEVEL_STATES.md).
#  지연 청크의 과거 신호도 현재 판단의 근거로 더해진다.
#
#  청크 파일 보관:
#     실험용 빌드  통화 종료까지 보관 (캡처·STT 실패 원인 규명에 필요)
#     최종 빌드    업로드 성공 즉시 삭제, 실패분만 큐에 유지
#     논문 서술: "실험 목적 한정 보관, 배포 빌드에서는 즉시 파기"


# ============================================================================
#  8. 실험 설계
# ============================================================================
#
#  입력 방식 두 가지를 모두 쓰고, 논문에 지표를 나란히 싣는다.
#
#     녹취 주입 (recording)  동일 입력 반복 → 탐지 성능의 신뢰구간
#                            6.3.1 정탐률 · 오탐률
#     실통화   (live_call)   되먹임이 실제로 일어난다는 증거
#                            6.3.2 능동 탐침 효과 (위험점수 Δ)
#
#  대응 조건:
#     대조군 ctrl_lv1   Lv1 만 작동. 경고만 하고 스크립트 없음 (기존 앱 수준)
#     실험군 exp_full   Lv2~Lv5 전부 작동 (온새미의 주장)
#
#  녹취 주입은 사기범이 반응할 수 없어 탐침 효과의 절반만 측정된다.
#  실통화는 재현성이 낮다 (말 속도·억양·잡음이 매번 다름) — 논문 한계에 명시.
#
#  빌드 분리 원칙:
#     실험용 빌드  5.3.2 에서 QA 통과 판정한 버전. 6.3~6.4 결과표는 전부 이 빌드
#     최종 빌드    6.5.1 가중치 보정을 반영해 6.5.2 에서 동결한 버전
#     보정 결과를 이미 수행한 실험에 소급 적용하지 않는다.
#     논문 5장 수치는 실험용 빌드 기준으로 적는다.


# ============================================================================
#  9. 미확정 — 아직 정하지 않았다
# ============================================================================
#
#   청크 중첩 구간 길이            3.1.2 STT 연동 후 실측하여 결정
#                                  (2초로 자르면 경계에서 단어가 잘린다.
#                                   0.5초쯤 겹쳐 자르는 방안을 실측 후 판단)
#   Lv1~Lv5 화면 구성              1.3.1
#   sensitive_pattern 승급 전용 화면  1.3.1
#   feedback.reaction 값 목록      2.3.2 인터랙션 구현 시 확정


# ============================================================================
#  10. 서버 코드 현황 — 알려진 결함 (2026-09-16 갱신)
# ============================================================================
#
#  아래는 3.1.1 골격에 남아 있는 문제다. 고친 뒤 이 목록을 지운다.
#
#   [4] SSE 구독 전 결과 유실
#       publish 는 그 시점 구독자에게만 보낸다.
#       앱이 스트림을 열기 전 첫 청크가 오면 결과가 사라진다.
#       세션별 최근 N건 버퍼를 두거나, 스트림 선연결을 명세에 못 박아야 한다.
#       (앱 쪽은 SSE 먼저 연결 후 녹음 시작하도록 짜서 우회하고 있지만,
#        서버 자체의 구조적 결함은 아직 남아있다.)
#
#   [7] 잔가지
#       @app.on_event("startup") 폐기 예정 → lifespan
#
#  --- 2026-09-16 에 고친 항목 (참고용, 재발 방지) ---
#   [2] DB 커넥션 누수 — get_conn() 호출부를 전부
#       `with closing(get_conn()) as conn, conn:` 로 감쌈.
#   [6] 동기 DB·파일 I/O 가 이벤트 루프를 막는 문제 — upload_chunk/end_session 의
#       db 호출과 오디오 파일 쓰기를 run_in_threadpool 로 오프로드.
#   ensure_session 동시 요청 시 PK 충돌 — SELECT 선확인을 없애고
#       INSERT 후 IntegrityError 를 잡는 방식으로 변경 (원자적).
#   AUDIO_DIR.mkdir / sess_dir.mkdir 에 parents=True 누락 — 추가.
#   청크가 0건인 세션은 타임아웃 워처가 영영 닫지 않던 문제 — db.last_chunk_time()
#       을 db.last_activity_time() 으로 교체, 청크가 없으면 session.started_at 사용.
#
#  --- 2026-09-17 에 고친 항목 ---
#   [5] 분석이 업로드 응답을 막는 구조 — STT(3.1.2) 연동과 함께 BackgroundTasks로
#       분리. 202 는 즉시 응답, STT·저장·SSE 발행은 analyze_and_publish 로 이동.
#   [1] 경로 조작 취약점 — validate_session_id() 로 UUID 형식만 허용하도록
#       세 엔드포인트(chunks/stream/end) 전부에 추가.
#   [3] SESSION_LIMIT_SEC 미구현 — timeout_watcher 가 session.started_at 기준
#       경과 시간도 같이 검사해 "session_limit" 사유로 강제 종료하도록 추가.
# ============================================================================


# ============================================================================
#  변경 이력
# ============================================================================
#
#  v0.2 | 2026-09-21 | 레벨 Lv0~Lv5 확장, 가중치 표 v1.1·상태 전이 v1.0 반영, 청크 2초 확정
# ============================================================================
